<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Supermarket App</title>

    <!-- BOOTSTRAP -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- BACKGROUND IMAGE STYLE -->
    <style>
        body {
            background: url('/images/backgroundimage.png') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            margin: 0;
            padding: 0;
        }

        /* Optional: Soft dark overlay to make text readable */
        .overlay {
            background: rgba(0, 0, 0, 0.45);
            position: absolute;
            inset: 0;
        }

        .content-box {
            position: relative;
            z-index: 5;
        }
    </style>
</head>

<body>

<!-- DARK OVERLAY (optional, makes text pop) -->
<div class="overlay"></div>

<!-- NAVBAR -->
<nav class="navbar navbar-expand-sm bg-dark navbar-dark position-relative" style="z-index: 10;">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Supermarket App</a>

        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a href="/register" class="nav-link">Register</a></li>
                <li class="nav-item"><a href="/login" class="nav-link">Login</a></li>
            </ul>
        </div>
    </div>
</nav>

<!-- MAIN CONTENT -->
<div class="container d-flex justify-content-center align-items-center content-box" style="height: 80vh;">
    <div class="text-center text-white">

        <h1 class="mb-4 fw-bold">WELCOME TO SUPERMARKET APP</h1>

        <div class="d-flex justify-content-center gap-4">
            <a href="/login" class="btn btn-primary btn-lg px-5 py-2">Login</a>
            <a href="/register" class="btn btn-success btn-lg px-5 py-2">Register</a>
        </div>

    </div>
</div>

</body>
</html>
